extends ../../layouts/base/layout

block content
  section.ticket
    .ticket__wrapper
      h1.visually-hidden Карточка объявления
      .ticket__content
        .ticket__img
          img(src=(offer.picture ? `/img/${offer.picture}` : null), srcset=(offer.picture ? `/img/${offer.picture.replace(/\./, `@2x.`)} 2x` : null), alt="Изображение товара")
        .ticket__info
          h2.ticket__title #{offer.title}
          .ticket__header
            p.ticket__price
              span.js-sum #{offer.sum}
              |  ₽
            p.ticket__action #{offer.type === 'buy' ? 'КУПЛЮ' : 'ПРОДАМ'}
          .ticket__desc
            p #{offer.description}
          .ticket__data
            p
              b Дата добавления:
              span 20 ноября 2019
            p
              b Автор:
              a(href="#") Денис Шкатулкин
            p
              b Контакты:
              a(href="mailto:shkatulkin@ya.ru") shkatulkin@ya.ru
          ul.ticket__tags
            each cat in offer.category
              li
                a.category-tile.category-tile--small(href="#")
                  span.category-tile__image
                    img(src="/img/cat.jpg", srcset="/img/cat@2x.jpg 2x", alt="Иконка категории")
                  span.category-tile__label #{cat}

      .ticket__comments
        h2.ticket__subtitle Коментарии
        .ticket__comment-form
          form.form.comment-form(action="", method="post", autocomplete="off")
            .comment-form__header
              a.comment-form__avatar.avatar(href="#")
                img(src="/img/avatar.jpg", srcset="/img/avatar@2x.jpg 2x", alt="Аватар пользователя")
              p.comment-form__author Вам слово
            .comment-form__field
              .form__field
                textarea#comment-field.js-field(name="text", cols="30", rows="10", maxlength="400", minlength="50") Нормальное вообще кресло! А как насч
                label(for="comment-field") Текст комментария
                span Обязательное поле
            button.comment-form__button.btn.btn--white.js-button(type="submit" disabled="") Отправить

        if offer.comments.length > 0
          .ticket__comments-list
            ul.comments-list
              each comment in offer.comments
                li
                  .comment-card
                    .comment-card__header
                      a.comment-card__avatar.avatar(href="#")
                        img(src="/img/avatar02.jpg", srcset="/img/avatar02@2x.jpg 2x", alt="Аватар пользователя")
                      p.comment-card__author Георгий Шпиц
                    .comment-card__content
                      p #{comment.text}
        else
          .ticket-message
            p У этой публикации еще нет ни одного комментария.
